repos:
  # General file checks
  - repo: https://github.com/pre-commit/pre-commit-hooks
    rev: v4.5.0
    hooks:
      - id: check-added-large-files
        args: ['--maxkb=10000']
      - id: check-case-conflict
      - id: check-merge-conflict
      - id: check-yaml
      - id: end-of-file-fixer
      - id: trailing-whitespace
      - id: mixed-line-ending

  # Secret detection
  - repo: https://github.com/Yelp/detect-secrets
    rev: v1.4.0
    hooks:
      - id: detect-secrets
        args: ['--baseline', '.secrets.baseline']
        exclude: package.lock.json

  # Python linting (must run before black so import sorting sticks)
  - repo: https://github.com/astral-sh/ruff-pre-commit
    rev: v0.1.14
    hooks:
      - id: ruff
        args: [--fix]
        files: ^analysis/(src|tests)/.*\.py$

  # Python formatting
  - repo: https://github.com/psf/black
    rev: 24.1.1
    hooks:
      - id: black
        language_version: python3.11
        files: ^analysis/(src|tests)/.*\.py$

  # Python type checking - Disabled due to many type issues, run manually
  # - repo: https://github.com/pre-commit/mirrors-mypy
  #   rev: v1.8.0
  #   hooks:
  #     - id: mypy
  #       files: ^analysis/src/.*\.py$
  #       args: [--ignore-missing-imports, --no-strict-optional]
  #       additional_dependencies: [
  #         types-pyyaml,
  #         numpy,
  #         pandas-stubs,
  #       ]

  # Prevent debug artifacts in Python - Disabled (many false positives in docstrings)
  # Enable when docstring print() examples are cleaned up
  # - repo: local
  #   hooks:
  #     - id: no-print-statements
  #       name: Prevent print() in production code
  #       entry: '^\s+print\('
  #       language: pygrep
  #       files: ^analysis/src/ced_ml/.*\.py$
  #       exclude: (cli/|utils/logging|/__init__\.py$)

  # Prevent .env files from being committed
  # Disabled - .gitignore entry triggers false positive
  # - repo: local
  #   hooks:
  #     - id: no-env-files
  #       name: Prevent .env files
  #       entry: '^\.env$'
  #       language: pygrep
  #       files: '\.env$'
  #       exclude: '(\.env\.example$|\.gitignore$)'
