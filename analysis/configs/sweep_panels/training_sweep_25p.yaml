# Training config for panel sweep: 25-protein panel
# Sequential addition in RRA rank order. Same holdout splits as phase3.
_base: training_config.yaml

features:
  feature_selection_strategy: fixed_panel
  fixed_panel_csv: "sweep_panels/panel_25p.csv"

optuna:
  n_trials: 50
