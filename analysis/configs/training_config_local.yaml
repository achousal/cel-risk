# Local overrides -- lightweight settings for development and smoke tests.
# Production defaults are inherited from training_config.yaml via _base.
_base: training_config.yaml

cv:
  folds: 3
  repeats: 5
  n_jobs: 1

optuna:
  n_trials: 40
  sampler: tpe
  pruner: hyperband
  pruner_n_startup_trials: 5
  n_jobs: 1

evaluation:
  n_boot: 100

lr:
  max_iter: 5000
