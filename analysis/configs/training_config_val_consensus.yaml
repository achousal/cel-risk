# Training config for consensus panel validation (seeds 105-109)
# Validates discovery run 20260309_155812 consensus panel on fresh seeds.
# All settings inherited from training_config.yaml except feature selection.
_base: training_config.yaml

features:
  feature_selection_strategy: fixed_panel
  fixed_panel_csv: "fixed_panel_consensus_20260309_155812.csv"
