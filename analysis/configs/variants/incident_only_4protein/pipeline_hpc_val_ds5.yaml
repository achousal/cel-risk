# Pipeline: 4-protein, incident-only, 1:5 downsample, val mode
_base: ../../pipeline_hpc.yaml

paths:
  splits_dir: ../splits_inc_val_ds5

configs:
  training: configs/variants/incident_only_4protein/training_config_ds5.yaml
  splits: configs/variants/incident_only_4protein/splits_config_val_ds5.yaml

pipeline:
  optimize_panel: false
  consensus: false
  permutation_test: false
