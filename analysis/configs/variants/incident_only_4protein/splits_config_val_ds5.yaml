# Incident-only + 4-protein validation splits (1:5 control downsampling)
# Pairs with training_config_ds5.yaml (class_weight=none; downsample is the imbalance remedy).
_base: ../../splits_config.yaml

mode: development
scenarios:
  - IncidentOnly

n_splits: 11
seed_start: 20

train_control_per_case: 5
eval_control_per_case: 5
