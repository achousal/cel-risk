# Pipeline config for consensus panel validation (100 Optuna trials)
# Same splits as 50-trial variant, different training config.
_base: pipeline_hpc_val_consensus.yaml

configs:
  training: configs/training_config_val_consensus_100t.yaml
