# Pipeline config for Phase 3: holdout evaluation
# Trains on dev portion (70%) with fixed 7-protein panel, evaluates on 30% holdout.
# Inherits HPC settings from pipeline_hpc.yaml.
_base: pipeline_hpc.yaml

configs:
  training: configs/training_config_holdout.yaml
  splits: configs/splits_config_holdout.yaml

pipeline:
  optimize_panel: false
  consensus: false
  permutation_test: false
